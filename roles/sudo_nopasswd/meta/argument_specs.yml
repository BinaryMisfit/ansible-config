---
argument_specs:
  main:
    short_description: Give specified user sudo access
    options:
      user_to_sudo:
        type: 'str'
        required: true
        description: 'A valid username existing on the system'
